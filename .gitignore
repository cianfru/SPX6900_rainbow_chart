# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# X bot dry-run artifact
bot-preview*.png
bot-preview*.mp4

# Recap previews are reviewed live on the control panel (/control → Monthly
# recap), never committed. --dry-run writes these throwaway PNGs locally.
recap-preview-*.png
public/recap/

# local card preview renders (keep dune/out/*.csv as provenance)
dune/out/*.png

# Throwaway Playwright/render probes written to the repo root while debugging the 3D city.
# They are scratch, never part of the build, and one slipped into a commit once already.
v.mjs
dbg*.mjs
meas.mjs
chk*.mjs
check*.mjs
lab.mjs
